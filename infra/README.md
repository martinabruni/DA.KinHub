# Azure

I parametri sono placeholder. Prima creare ACR, Key Vault, PostgreSQL, App Service, Static Web App e identità gestita tramite pipeline OIDC. Le password arrivano da secret sicuri; non inserirle nei parametri.
