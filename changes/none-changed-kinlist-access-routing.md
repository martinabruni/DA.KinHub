---
type: changed
area: backend
breaking: false
issue: none
---

## it
KinList ora crea o riusa un profilo applicativo da `(iss, oid)`, verifica la membership familiare attiva nel bootstrap post-login, espone la policy `Family` e mantiene offline soltanto la shell pubblica senza dati personali.

## en
KinList now creates or reuses an application profile from `(iss, oid)`, verifies the active family membership during the post-login bootstrap, exposes the `Family` policy, and keeps only the public shell available offline without personal data.
