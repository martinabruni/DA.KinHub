---
type: fixed
area: infra
breaking: false
issue: none
---

## it
Corretti i workflow di deploy per usare gli argomenti Azure CLI corretti nella gestione della firewall rule di PostgreSQL Flexible Server, distinguendo `--server-name` dal nome della regola.

## en
Fixed the deployment workflows to use the correct Azure CLI arguments when managing the PostgreSQL Flexible Server firewall rule, separating `--server-name` from the firewall rule name.
