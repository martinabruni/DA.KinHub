---
type: fixed
area: infra
breaking: false
issue: none
---

## it
Corretto il workflow `deploy-infrastructure.yml` per risolvere in modo affidabile nome e object id dell'amministratore Entra di PostgreSQL, evitando parametri vuoti che invalidavano il deployment Bicep.

## en
Fixed `deploy-infrastructure.yml` to resolve the PostgreSQL Entra administrator name and object id reliably, preventing empty parameters that invalidated the Bicep deployment.
