---
type: changed
area: tooling
breaking: false
issue: none
---

## it

L'harness ora verifica che ogni route di una HTTP Function sia documentata in `openapi.yaml`, rendendo obbligatorio aggiornare il contratto statico con ogni nuovo endpoint.

## en

The harness now verifies that every HTTP Function route is documented in `openapi.yaml`, requiring the static contract to be updated with every new endpoint.
