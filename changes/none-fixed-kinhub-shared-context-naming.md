---
type: fixed
area: backend
breaking: true
issue: none
---

## it
Bootstrap, contesto famiglia, autorizzazione e telemetria condivisi usano ora nomenclatura e route KinHub; rimossi i contratti `/api/kinlist/bootstrap` e `/api/kinlist/family-context` attribuiti erroneamente al KinService.

## en
Shared bootstrap, family context, authorization, and telemetry now use KinHub naming and routes; the incorrectly KinService-scoped `/api/kinlist/bootstrap` and `/api/kinlist/family-context` contracts were removed.
