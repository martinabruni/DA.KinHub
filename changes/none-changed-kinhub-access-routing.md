---
type: changed
area: backend
breaking: false
issue: none
---

## it
KinHub ora crea o riusa un profilo applicativo da `(iss, oid)`, verifica la membership familiare attiva nel bootstrap post-login, espone la policy `Family` condivisa e mantiene offline soltanto la shell pubblica senza dati personali.

## en
KinHub now creates or reuses an application profile from `(iss, oid)`, verifies active family membership during post-login bootstrap, exposes the shared `Family` policy, and keeps only the public shell available offline without personal data.
