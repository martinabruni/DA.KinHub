# Docs sync

Valida le guide Markdown bilingui, i metadati e il collegamento tra route e guida; `sync` genera un JSON importabile dal frontend mantenendo Markdown come unica fonte.

```bash
npm run docs:validate
npm run docs:sync
```
